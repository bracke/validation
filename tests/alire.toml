name = "validation_tests"
description = "AUnit test suite for the validation library"
version = "0.1.0-dev"

authors = ["Bent Bracke"]
maintainers = ["Bent Bracke <bent@bracke.dk>"]
maintainers-logins = ["bracke"]
licenses = "MIT OR Apache-2.0 WITH LLVM-exception"
website = ""
tags = []

executables = ["tests"]

[[depends-on]]
gnat_native = "=15.2.1"

[[depends-on]]
aunit = "^26.0.0"

[[depends-on]]
validation = "*"

[[pins]]
validation = { path = ".." }

# Build dependencies in the development profile (-Og) too, not the default
# release (-O3), so roots that share these crates share object files rather
# than forcing each other into full rebuilds.
[build-profiles]
"*" = "development"
