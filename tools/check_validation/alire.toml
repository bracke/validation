name = "check_validation"
description = "Ada dependency-boundary + release checks for the validation library"
version = "0.1.0-dev"
authors = ["Bent Bracke"]
maintainers = ["Bent Bracke <bent@bracke.dk>"]
maintainers-logins = ["bracke"]
licenses = "MIT"
tags = ["tool", "release", "check"]

executables = ["check_validation"]

[[depends-on]]
gnat_native = "=15.2.1"

[[depends-on]]
project_tools = "*"

[[pins]]
project_tools = { path = "../../../project_tools" }

[build-profiles]
"*" = "development"
